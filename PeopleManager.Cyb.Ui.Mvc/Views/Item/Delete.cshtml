@model ShopItem

@{
    ViewData["Title"] = $"Delete {Model.ItemName}";
}

<h1>@ViewData["Title"]</h1>

<p>Are you sure you want to delete @Model.ItemName</p>

<form method="post">
    <a asp-action="Index">No</a>
    <button type="submit" class="btn btn-primary">Yes</button>
</form>